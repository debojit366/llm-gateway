🚀 How to Run (1-Click Deployment)The complete pipeline, along with databases, is fully containerized using Docker Compose.1. Spin up the entire infrastructure:Bashdocker-compose up --build
This single command spins up:llm-gateway-container (FastAPI backend at port 8000)redis-container (Redis cache at port 6379)frontend-dashboard (Vite production environment at port 5173)2. Verify Server Health:Open your browser or Postman and check the background telemetry logs:HTTPGET http://localhost:8000/api/v1/analytics/dashboard?range=today
Headers: { "X-API-Key": "your_configured_gateway_key" }
3. Open Live Visual Insights Dashboard:Navigate to http://localhost:5173 to track real-time visual charts, active hit percentage stats, total cost saved counters, and individual user token distributions with zero manual refresh required!📌 API Endpoints SummaryMethodEndpointDescriptionAuth RequiredPOST/api/v1/chat/completionsGenerates a streaming text output using the fallback/semantic cache network.YesGET/api/v1/analytics/dashboardFetches active arrays (hits, misses, labels) sorted by selected timeline range.Yes
