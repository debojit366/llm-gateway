version: '3.8'

services:
  gateway:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-gateway-container
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./app:/code/app 
    restart: always